{
  "name": "Token",
  "prefix": "vault",
  "description": "The following example shows authenticating to Vault via a Token.",
  "config": {
    "vault.login.by": "Token",
    "vault.address": "https://vault.example.com",
    "vault.token": "sdifgnabdifgasbffvasdfasdfadf"
  },
  "connectorConfig": {
    "username": "${vault:secret/test/someconnector:username}",
    "password": "${vault:secret/test/someconnector:password}"
  },
  "tip": "Some of the settings of this config provider can be configured via environment variables. Take a look at the documentation to see which settings could be replaced by environment variables."
}